pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls'
            }
        }
        tage('test') {
            steps {
                sh 'npm i'
            }
        }
    }
}
